language: go
go:
  - "1.10"
before_install:
  - curl -sSL https://raw.githubusercontent.com/golang/dep/master/install.sh | sh
  - sudo apt-get install -y gcc-4.8 gobjc++-mingw-w64
install:
  - dep ensure
